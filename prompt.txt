Fix the 'Null check operator used on a null value' crash in the collaborative list and request management screens. Find where collaboration requests or user details are rendered. Remove forced null checks (!) on fields like avatarUrl, username, or IDs. Replace them with safe fallbacks (e.g., variable ?? 'default' or rendering a fallback widget). Do not run tests or bash scripts, just apply the UI safety fixes directly.